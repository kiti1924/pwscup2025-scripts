# Review document for PWS Cup 2025

## Anonymization
- Team 01
    - syntheaで作られた合成データ
        - 
- Team 02
    - 
- Team 19
- Team 23
    - 
## Attack
- Team 01
    - 近傍からのハンガリアンマッチング(距離ベース)による攻撃
        - ただし、距離が近すぎるものは除外する
        - 距離計算に機械学習(リッジ回帰など)を利用
            - Cでの予測誤差とAでの予測誤差のずれを全数値属性に対して計算し、距離とする
        - 重み付けにはWS距離(AとCのデータ分布差)、相関絶対誤差和(他の属性との相関でのAとCの差)

- Team 02
    - ランダムフォレストによる攻撃(一部のチームにのみ有効/予備戦の匿名化処理をランダムフォレストで学習する)
        - 予備戦のA、C、Zデータから特徴量ベクトルを作成(カテゴリのone-hot化、数値の標準化)
        - Aの各行にC全体から特徴量を計算する
            - コサイン類似度のmin/max/avg/std、マンハッタン距離のmin/avg/std、カテゴリ一致数のavg
        - Zファイルを目的変数として特徴量を用いてランダムフォレストで学習する
            - AA, CCから推論を行うことが可能
    
    - クラスタリングによる攻撃
        - Cを用いたクラスタリング結果をAにあてはめ、Bを推測
        - 距離ベースのサンプル攻撃とほぼ同等精度

- Team 03
    - AIエージェントによる実装
    - サンプルのMIA攻撃が十分に有効：AI
    - 上位チームにはランダムに攻撃

- Team 05
    - Gower距離による攻撃(仮定：asthma_flagを目的変数とした構造を壊さない加工をしている)
        - LR分析の際にロジスティック回帰のランキング(CC/AAの重要度ランク)を出力
        - 重要度が大きいもの、A→Cデータでランク変動があるものに列を絞って距離計算をする
    - 相互最近傍による攻撃
        - A/Cの両方を正規化、one-hot化し、マンハッタン距離による距離を計算
            - 特徴量ベクトルの絶対値の総和を利用
        - A→C、C→Aの両方で互いに最も近いペア(相互最近傍)はメンバー

- Team 06
    - 4種の攻撃結果を比較し、最善の結果を提出
        - サンプルによるMIA攻撃(仮定：Bを加工した場合近いレコードが残るはず)：そこそこ有効
        - 数値列を順位化し、最近傍探索：チーム23に有効
            - C→Aの最近傍探索(仮定：BからCを作成していれば、B→Cでの対応行は近いはず)
            - 順位化(仮定：数値属性にはノイズが乗っているはず)
        - Cと遠い値を持つ行を削除：チーム02に有効
            - AでCのどの値とも遠い値はA\B(仮定：BからCを作成していれば、B→Cの値は近い)
            - A/Cに対して、数値のみでAの各行からCへの距離を計算、最も近い行を出力する
        - CからLRモデルXを利用(仮定：XにBを与えると確信度が高い)：あまり有効でなかった

- Team 08
    - 

## Analysis
- Team 01

## DataScience
- Team 01